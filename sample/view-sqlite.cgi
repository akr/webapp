#!/usr/bin/env ruby

# modify above line to `#!/absolute/path/to/ruby' if default ruby binary is not suitable.

load "view-sqlite.rb"

# modify above line to `load "/absolute/path/to/view-sqlite.rb"' if you copy this file.
